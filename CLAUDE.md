# ウェアラブル実測ログ(サイト名は未定)

## 概要
ウェアラブル機器(スマートウォッチ、活動量計など)とイヤホンを、実際に使って測った数値で紹介する個人メディア。
最初の企画は「Apple Watch SE 3 と Fitbit Air の併用」で、連載名は「ウェアラブルデバイスの最適解を考える」(記事の冒頭とタグに入れる。サイト名ではない)。
運営者は医師(脳神経内科)で、データに基づく正確な記事を届けることを大切にする。

## ターゲットユーザー
次の3タイプのすべてを読者と考える。
- ガジェットが好きで、スペックや実測の違いを細かく知りたい人
- ガジェットに詳しくないが、買う前に参考にしたい一般の人
- 健康や運動の記録に関心がある人

## アプリのゴール
### WHAT(何を)
- 自分の記録と発信の場を持ち、ガジェット記事を書き続ける。
- カタログの数字ではなく、実測に基づいた比較を、一人称の体験として淡々と伝える。

### HOW(どうなってもらいたいか)
- 読者が、購入前に「実際に使うとどうか」を判断できる。
- 「なぜ SE 3 と Fitbit Air なのか」が、冒頭の結論で分かる。詳しい人は、続く実測記事で確かめられる。

## 主要機能
### 必須機能(MVP)
- 記事1本目「Apple Watch SE 3 と Fitbit Air を比較してみた【結論・スペック・価格編】」の公開(サイト名は載せない)
  - 結論を先に書く:この組み合わせがよい。理由は、互いの性能を補い合えることと、価格の面で有利なこと。
  - 補完の内容は、機能と使い勝手で書く(例:バッテリー、画面の有無、着け替え)。効能はうたわない。
  - 実測編の予定を最後に書く。
- トップページを記事一覧にする
- このサイトについて(about):目的、運営者の表示名と簡単な紹介、お問い合わせへのリンク、免責(医療的な効能をうたわない旨)
- プライバシーポリシー(privacy):短く。お問い合わせフォームの取り扱い、外部サービス(Google Fonts など)の利用、追記の予告
- お問い合わせ:Google フォームを使う(ログイン不要、名前欄なし、メールアドレスは任意)
- 記事を Markdown で書いて公開できる仕組み(実装済み)

### 追加予定機能
- 実測編の記事(Fitbit Air 到着後の2026年10月以降):測定方法、歩数、ワークアウト、睡眠など
- データのグラフ・比較表を、専用ページで公開(記録が始まってから)
- iOS ショートカット → Google Apps Script でのデータ自動入力
- アフィリエイト(Amazon アソシエイト):アクセスと記事が増えてから申請する
- サイト名とデザインの確定、独自ドメイン、イヤホン記事

## 技術仕様
- 公開:GitHub Pages(無料・公開リポジトリ)。URLは `https://yy-devlog.github.io/`(リポジトリ名は `yy-devlog.github.io`)
- 記事:`articles/` に Markdown で書き、Python(`scripts/build_site.py`)で HTML に変換。GitHub Actions が自動で公開
- データ:Google スプレッドシート(公開用シート)→ 自動で `data/log.json` に変換 → Chart.js でグラフ表示
- GitHub のユーザー名は `yy-devlog`。コミットのメールは noreply を使い、本物のメールアドレスは公開しない

### データベース設計(該当する場合)
該当なし(会員機能なし、読者の個人情報は保存しない)。
- お問い合わせは Google フォームで受ける。回答は運営者のみが見られる。
- スプレッドシートの中身は公開される前提のため、私的な内容は書かない。

## 執筆・表示の方針
- 一人称、実体験、誇張しない、淡々としたトーン。
- 医療的な効能や診断をうたわない。機能は「搭載されている」「自分はこう使っている」にとどめる(景品表示法・薬機法への配慮)。
- 実測で結論と違う結果が出たら、そのまま書く。
- 価格は「最低価格の構成」を明記し、確認した年月を入れる。公開直前に、公式ページで再確認する。
- アフィリエイトを始めるまでは、広告の表記は不要。始める際に、表記と各ページを更新する。

## UI/UX方針
- ダーク基調。見出しは Fraunces、本文とデータは Inter。
- アクセントカラー:Apple Watch = 青系(#8FA8C7)、Fitbit Air = 紫系(#B98BD9)。
- 冒頭に結論、詳細は後半へ。スマートフォンでも読みやすく。

## 開発進捗
### 済み
- サイトの土台、記事の仕組み、公開の仕組み、CSV 変換の修正
- トップを記事一覧に作り直し、実測データのページは `compare.html`(記録開始まで非公開)に移した
- 記事1本目「結論・スペック・価格編」を書いた(`draft: true`。価格の確認待ち)
- about・privacy を新しい内容に書き直した(運営者名・フォームURL・制定日が未記入)
- 広告・アフィリエイトの表記を外した
- サイト名の仮表示は「yy-devlog」(`scripts/build_site.py` の `SITE_NAME` で一括変更できる)
- GitHub にはまだ push していない(ローカルのみ)

### 未確定(確認待ち)
- サイト名(「ガジェログ」は既存サイトがあるため不可。他の案は「ソクログ」など)
- 運営者の自己紹介文(表示名は「Yuki」に決定済み。職業を書くかどうかも決める)
- Google フォームの URL(フォームの設計案は作成済み)
- 確認済み(2026年9月24日、公式ページ):SE 3 は 41,800円から(資料の ¥37,800 は公式では確認できず)、Series 12 は 71,800円から、Ultra 4 は 142,800円から、Fitbit Air は 16,800円(発売 2026年5月26日)
- 確認済みの仕様:SE 3 の重さ・センサー・バッテリー・非対応機能、Fitbit Air の重さ・バンド・SpO2・心房細動の通知、Google Health と Apple ヘルスケアの双方向同期
- 確認できず本文から外した内容:SE 3 の睡眠時無呼吸通知、Fitbit Air の5分急速充電・HRV 測定、Google Health v5.05 の版数、昼寝の誤記録の報告(出典を確認できたら戻す)
- フォントを、もう少し親しみやすいものに変更する(今後)

### 次のステップ
1. 未確定の項目を埋める(運営者の表示名、Google フォームの作成と URL、SE 3・Fitbit Air の価格の確認)
2. 記事の TODO を確認して `draft: true` を外す
3. GitHub でリポジトリ `yy-devlog.github.io` を作成し、push、Pages の Source を GitHub Actions に設定
4. Fitbit Air 到着後(2026年10月〜):測定方法の記事 → 実測編 → `compare.html` の公開
